# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "kiranshahi.github.io"

[setup]
script = '''
#!/usr/bin/env bash
set -e

echo "Updating package list..."
sudo apt-get update

echo "Installing dependencies..."
sudo apt-get install -y \
    ruby-full \
    build-essential \
    zlib1g-dev \
    git

echo "Installing Bundler and Jekyll..."
gem install bundler jekyll

echo "Installing project gems..."
bundle install

echo ""
echo "===================================="
echo "Jekyll environment is ready!"
echo "Run:"
echo "bundle exec jekyll serve --host 0.0.0.0"
echo "===================================="'''
